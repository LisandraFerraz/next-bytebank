import { endpoints } from "../../app/api/_environment/endpoints";

export const UseTransactions = () => {
  // Envia dinheiro
  const sendMoney = () => {};

  // Adiciona dinheiro
  const addMoney = () => {};

  // Calcula câmbio
  const currencyExc = () => {};

  // Pede empréstimo
  const applyLoan = () => {};
};
